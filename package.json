{
  "name": "spotify-api",
  "version": "1.0.0",
  "description": "The goal of this application is to query the spotify api and simply return the top 5 most listened to songs. Add these songs to a database and then save them.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "spotify-web-api-node": "^5.0.2"
  }
}
